# 🧪 اختبار نظام التخزين الدائم

دليل خطوة بخطوة للتحقق من أن جميع البيانات تُحفظ بشكل صحيح.

---

## ✅ اختبار 1: حفظ الطلب الجديد

### الخطوات:
1. **افتح التطبيق** بحساب Shipping Company:
   - البريد: `dpa-community@dpa.gov.eg`
   - كلمة المرور: `12345`

2. **انقر على "طلبات" في القائمة الجانبية**

3. **انقر على زر "طلب جديد فى المخازن" (الزر الأزرق)**

4. **ملئ النموذج:**
   - اختر مخزن من الخريطة
   - أدخل التفاصيل

5. **انقر "إرسال الطلب"**

### التحقق:
```
✅ يجب أن تظهر رسالة نجاح
✅ يجب أن يظهر الطلب في القائمة أسفل النموذج
```

---

## ✅ اختبار 2: الحفاظ على البيانات عند التحديث

### الخطوات:
1. **ادخل طلباً جديداً** (من اختبار 1)

2. **اضغط F5 أو انقر الزر "تحديث"** ♻️
   (أو اذهب إلى صفحة أخرى وعد مجدداً)

3. **تحقق من الطلبات**

### التحقق:
```
✅ جميع الطلبات التي أدخلتها يجب أن تكون موجودة
✅ لا يجب فقد أي بيانات
```

---

## ✅ اختبار 3: الحفاظ على البيانات عند الخروج

### الخطوات:
1. **ادخل طلب جديد** (أو عدّل أحد الطلبات الموجودة)

2. **انقر على صورة الملف الشخصي** (أيقونة المستخدم بالأعلى)

3. **انقر "تسجيل الخروج"**

4. **أعد تسجيل الدخول** باستخدام نفس الحساب

5. **ادخل إلى صفحة الطلبات**

### التحقق:
```
✅ جميع الطلبات التي أدخلتها يجب أن تكون موجودة
✅ لا يجب أن تختفي البيانات
```

---

## ✅ اختبار 4: المزامنة بين العلامات (متقدم)

### الخطوات:
1. **افتح التطبيق في عاموتين مختلفتين**
   - العلامة 1: `localhost:5173`
   - العلامة 2: `localhost:5173`

2. **في العلامة 1: أدخل طلب جديد**

3. **مباشرة، انقر على الفلتر في العلامة 2** أو انتظر ثانية

### التحقق:
```
✅ الطلب الجديد يجب أن يظهر في العلامة 2 تلقائياً
✅ إذا لم يظهر، يمكنك تحديث باليد F5
```

---

## 🔍 اختبار متقدم: فحص localStorage

اتبع هذه الخطوات للتحقق من البيانات المحفوظة مباشرة:

### نقطة 1: افتح Developer Tools
```
اضغط: Ctrl + Shift + I  (Windows)
أو      Cmd + Option + I (Mac)
أو      F12 (جميع الأنظمة)
```

### نقطة 2: اذهب إلى Application Tab
```
الأعلى → Application (أو Storage في Firefox)
       → Local Storage
       → localhost:5173
```

### نقطة 3: ابحث عن المفاتيح الآتية:

```
🔍 key: 'port-navigator-data'
   value: {
     "users": [...],
     "requests": [...],
     "complaints": [...],
     "warehouses": [...],
     "currentUser": {...},
     "lastSync": "2026-02-08T..."
   }

🔍 key: 'port-navigator-user'
   value: {
     "id": "...",
     "email": "dpa-community@dpa.gov.eg",
     ...
   }

🔍 key: 'port-navigator-sync'
   value: "2026-02-08T12:34:56.789Z"
```

### التحقق:
```
✅ يجب أن تجد هذه المفاتيح الثلاثة
✅ يجب أن تحتوي على بيانات صحيحة
✅ آخر طلب أدخلته يجب أن يكون في 'requests' array
```

---

## 🧪 اختبار الـ Console (متقدم جداً)

إذا كنت مطوراً، يمكنك اختبار مباشرة:

### الخطوة 1: افتح Console Tab
```
F12 → Console (العلامة الثانية في Developer Tools)
```

### الخطوة 2: انسخ والصق الأوامر التالية:

```javascript
// 1️⃣ حصل على الإحصائيات
// (لاحظ: StorageService قد لا يكون متاح مباشرة في Console)

// بدلاً من ذلك، افحص localStorage مباشرة:
const data = JSON.parse(localStorage.getItem('port-navigator-data'));
console.log('البيانات المحفوظة:', data);

// 2️⃣ عدد الطلبات
console.log('عدد الطلبات:', data.requests.length);

// 3️⃣ عدد الشكاوى
console.log('عدد الشكاوى:', data.complaints.length);

// 4️⃣ آخر وقت تحديث
console.log('آخر تحديث:', data.lastSync);

// 5️⃣ حجم البيانات بالبايت
const size = JSON.stringify(data).length;
console.log('حجم البيانات:', size, 'bytes');
console.log('حجم البيانات:', (size / 1024).toFixed(2), 'KB');
```

---

## 🔄 اختبار النسخ الاحتياطية (Backup)

إذا أردت نسخ بيانات احتياطية يدويسة:

```javascript
// 1️⃣ تحميل البيانات
const data = localStorage.getItem('port-navigator-data');

// 2️⃣ نسخ للحافظة
copy(data);
// أو
navigator.clipboard.writeText(data);

// 3️⃣ حفظ في ملف:
const element = document.createElement('a');
element.setAttribute('href', 'data:text/plain;charset=utf-8,' + encodeURIComponent(data));
element.setAttribute('download', `backup-${new Date().toISOString()}.json`);
element.style.display = 'none';
document.body.appendChild(element);
element.click();
document.body.removeChild(element);
```

---

## 🔴 اختبار: حذف البيانات (احذر!)

⚠️️ **تنبيه: هذا سيحذف جميع البيانات المحفوظة!**

```javascript
// احذف كل شيء (بحذر!)
localStorage.removeItem('port-navigator-data');
localStorage.removeItem('port-navigator-user');
localStorage.removeItem('port-navigator-sync');

console.log('تم حذف البيانات');

// لاحقاً: لتحديث الصفحة
// location.reload();
```

---

## 📋 جدول الاختبارات

| اختبار | الطريقे | النتيجة المتوقعة | الحالة |
|--------|---------|-------------------|--------|
| حفظ الطلب | إدخال + F5 | الطلب موجود | ✅ |
| الخروج/الدخول | Logout → Login | البيانات باقية | ✅ |
| عليقات متعددة | بناء علامتين | المزامنة تلقائية | ✅ |
| التحقق من localStorage | Console → localStorage | المفاتيح موجودة | ✅ |
| حجم البيانات | `JSON.stringify().length` | < 5MB | ✅ |

---

## 🐛 استكشاف الأخطاء

### المشكلة: الطلبات تختفي بعد التحديث

**الحل:**
```javascript
// تحقق من localStorage
console.log(localStorage.getItem('port-navigator-data'));

// إذا كانت فارغة، تحقق من Console للأخطاء:
// Ctrl + Shift + I → Console
// ابحث عن رسائل خطأ حمراء
```

### المشكلة: البيانات لا تُحفظ على الإطلاق

**الحل:**
1. تحقق من أن المتصفح يسمح بـ localStorage
2. تحقق من وضع التشفير الخاص (Private mode) - قد لا يسمح بـ localStorage
3. امسح cache وحاول مجدداً:
   ```
   Ctrl + Shift + Delete → Clear browsing data → localStorage
   ```

### المشكلة: البيانات مفسودة

**الحل:**
```javascript
// امسح وأعد التحميل
localStorage.clear();
location.reload();
```

---

## ✨ النجاح!

إذا أكملت جميع الاختبارات بنجاح:

```
🎉 نظام التخزين الدائم يعمل بشكل مثالي!
```

الآن جميع بيانات المستخدم:
- ✅ تُحفظ تلقائياً
- ✅ تستمر بعد الخروج
- ✅ تستمر بعد إغلاق المتصفح
- ✅ تستمر بعد إعادة تشغيل النظام

---

## 📞 مساعدة إضافية

إذا واجهت مشاكل:
1. افتح Console (F12)
2. انسخ أي رسائل خطأ حمراء
3. شارك مع الفريق للمراجعة

جزاك الله خيراً لاستخدامك نظام التخزين الجديد! 🚀
