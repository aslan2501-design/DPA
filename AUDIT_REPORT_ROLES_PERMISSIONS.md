# تقرير مراجعة وتحديث الأدوار والصلاحيات
## Role-Based Access Control (RBAC) Audit & Implementation Report

**التاريخ:** 8 فبراير 2026  
**الحالة:** ✅ مكتمل  
**الإصدار:** 2.0  

---

## 1️⃣ ملخص تنفيذي (Executive Summary)

تم إجراء مراجعة شاملة لنظام الأدوار والصلاحيات في تطبيق ميناء دمياط الذكي والتأكد من:
- ✅ تطبيق الصلاحيات المحددة لكل دور بشكل صحيح
- ✅ عدم تسريب صفحات أو بيانات غير مخصصة
- ✅ الالتزام بمبدأ أقل صلاحية ممكنة (Least Privilege)

---

## 2️⃣ الأدوار المحددة في النظام

### 🔹 الدور 1: جهة خارجية - وكيل ملاحي (Maritime Agency)
**Classification:** `agency`

#### ✅ المسموح له:
- ✓ عرض لوحة التحكم الشخصية
- ✓ إنشاء وعرض طلبات التراكي (Trolley/Berthing)
- ✓ إرسال شكاوى الصيانة
- ✓ عرض شكواه الشخصية فقط
- ✓ عرض طلباته الشخصية فقط
- ✓ استخدام التجوال الافتراضي للميناء
- ✓ إرسال التقييمات والملاحظات

#### ❌ غير مسموح له (مخفي كلياً):
- ✗ تأجير مخزن (Dashboard + قائمة الطلبات)
- ✗ طلبات حجز المخازن
- ✗ عرض تحليل النحر والإطماء (Hydrography)
- ✗ إدارة الشكاوى أو تعديل حالاتها
- ✗ إدارة الطلبات أو الموافقة عليها
- ✗ لوحة الإدارة

**الصلاحيات في الكود:**
```typescript
// في rbac.ts:
canSeeWarehouseRequests() → false ❌
canSeeTrolleyRequests() → true ✅
canSeeWarehouseRentalInDashboard() → false ❌
canSeeTrolleyRequestInDashboard() → true ✅
canSeeHydrography() → false ❌
canEditComplaintStatus() → false ❌
canApproveRequests() → false ❌
```

---

### 🔹 الدور 2: جهة خارجية - شركة شحن وتفريغ (Shipping Company)
**Classification:** `shipping`

#### ✅ المسموح له:
- ✓ عرض لوحة التحكم الشخصية
- ✓ إنشاء وعرض طلبات تأجير المخازن
- ✓ إرسال شكاوى الصيانة
- ✓ عرض شكواه الشخصية فقط
- ✓ عرض طلباته الشخصية فقط
- ✓ استخدام التجوال الافتراضي للميناء
- ✓ إرسال التقييمات والملاحظات

#### ❌ غير مسموح له (مخفي كلياً):
- ✗ طلبات التراكي (Trolley/Berthing)
- ✗ عرض تحليل النحر والإطماء (Hydrography)
- ✗ إدارة الشكاوى أو تعديل حالاتها
- ✗ إدارة الطلبات أو الموافقة عليها
- ✗ لوحة الإدارة

**الصلاحيات في الكود:**
```typescript
// في rbac.ts:
canSeeWarehouseRequests() → true ✅
canSeeTrolleyRequests() → false ❌
canSeeWarehouseRentalInDashboard() → true ✅
canSeeTrolleyRequestInDashboard() → false ❌
canSeeHydrography() → false ❌
canEditComplaintStatus() → false ❌
canApproveRequests() → false ❌
```

---

### 🔹 الدور 3: الإدارة (Admin)
**UserType:** `ADMIN`

#### ✅ المسموح له:
- ✓ الوصول الكامل لجميع الأقسام
- ✓ إدارة جميع الشكاوى (عرض، تعديل الحالة)
- ✓ إدارة جميع الطلبات (عرض، موافقة/رفض)
- ✓ عرض التحليلات والتقارير
- ✓ لوحة الإدارة كاملة
- ✓ إدارة الموظفين والحسابات
- ✓ تعديل حالات البلاغات:
  - ⏳ قيد الانتظار
  - 🔄 قيد التنفيذ
  - ✅ تم الحل

#### ❌ غير مسموح له: لا شيء (صلاحيات كاملة)

**الصلاحيات في الكود:**
```typescript
// في rbac.ts:
canViewMap() → true ✅
canViewComplaints() → true ✅
canViewRequests() → true ✅
canViewDashboard() → true ✅
canViewAdmin() → true ✅
canEditStatus() → true ✅
canApproveRequests() → true ✅
isReadOnly() → false
```

---

### 🔹 الدور 4: موظف الميناء (DPA Staff)
**UserType:** `DPA_STAFF`

#### ✅ المسموح له:
- ✓ الوصول لجميع الأقسام (مثل Admin)
- ✓ إدارة الشكاوى (عرض، تعديل الحالة)
- ✓ إدارة الطلبات (معالجة، موافقة/رفض)
- ✓ عرض التحليلات والتقارير
- ✓ تعديل حالات البلاغات:
  - ⏳ قيد الانتظار
  - 🔄 قيد التنفيذ
  - ✅ تم الحل

#### ❌ غير مسموح له:
- ✗ لوحة إدارة النظام
- ✗ إدارة الحسابات والموظفين

**الصلاحيات في الكود:**
```typescript
// في rbac.ts:
canViewMap() → true ✅
canViewComplaints() → true ✅
canViewRequests() → true ✅
canViewDashboard() → true ✅
canViewAdmin() → false ❌
canEditStatus() → true ✅
canApproveRequests() → true ✅
isReadOnly() → false
```

---

### 🔹 الدور 5: الرئيس (Chairman)
**UserType:** `CHAIRMAN`

#### ✅ المسموح له:
- ✓ عرض لوحة التحكم
- ✓ عرض جميع الشكاوى (للمراجعة فقط)
- ✓ عرض جميع الطلبات (للمراجعة فقط)
- ✓ عرض التحليلات
- ✓ عرض التجوال الافتراضي

#### ❌ غير مسموح له:
- ✗ إنشاء أو تعديل شكاوى
- ✗ إنشاء أو معالجة طلبات
- ✗ تعديل حالات البلاغات
- ✗ الموافقة على الطلبات
- ✗ لوحة الإدارة

**الصلاحيات في الكود:**
```typescript
// في rbac.ts:
isReadOnly() → true (كل شيء read-only)
canEditStatus() → false ❌
canApproveRequests() → false ❌
```

---

## 3️⃣ الصلاحيات المطبقة في النظام

### جدول توزيع الصلاحيات الشامل

| الصلاحية | Maritime Agency | Shipping Company | Admin | DPA Staff | Chairman |
|---------|:---------------:|:----------------:|:-----:|:---------:|:--------:|
| عرض البلاغات | ✓ (خاصة به) | ✓ (خاصة به) | ✓ (الكل) | ✓ (الكل) | ✓ (الكل) |
| إنشاء بلاغ | ✓ | ✓ | ✗ | ✗ | ✗ |
| تعديل حالة البلاغ | ✗ | ✗ | ✅ (pending/in_progress/resolved) | ✅ | ✗ |
| عرض طلبات التراكي | ✓ (خاصة به) | ✗ | ✓ (الكل) | ✓ (الكل) | ✓ (الكل) |
| إنشاء طلب تراكي | ✓ | ✗ | ✗ | ✗ | ✗ |
| عرض طلبات المخازن | ✗ | ✓ (خاصة به) | ✓ (الكل) | ✓ (الكل) | ✓ (الكل) |
| إنشاء طلب مخزن | ✗ | ✓ | ✗ | ✗ | ✗ |
| الموافقة على الطلبات | ✗ | ✗ | ✅ | ✅ | ✗ |
| عرض التحليلات | ✗ | ✗ | ✓ | ✓ | ✓ (read-only) |
| لوحة الإدارة | ✗ | ✗ | ✓ | ✗ | ✗ |

---

## 4️⃣ حالات البلاغات والطلبات

### حالات البلاغات (Complaint Status)
```typescript
type ComplaintStatus = 'pending' | 'in_progress' | 'resolved';

// التفاصيل:
⏳ pending       - قيد الانتظار (تم الاستقبال، لم يتم البدء)
🔄 in_progress  - قيد التنفيذ (جاري المعالجة)
✅ resolved     - تم الحل (اكتمل البلاغ)
```

**من يمكنه تعديل الحالة:** Admin و DPA_STAFF فقط

### حالات الطلبات (Request Status)
```typescript
type RequestStatus = 'pending' | 'approved' | 'rejected' | 'in_progress';

// التفاصيل:
⏳ pending      - قيد الانتظار (تحت المراجعة)
✅ approved    - موافق عليه (تمت الموافقة)
❌ rejected    - مرفوض (لم يتم القبول)
🔄 in_progress - قيد التنفيذ (جاري المعالجة)
```

**من يمكنه تعديل الحالة:** Admin و DPA_STAFF فقط

---

## 5️⃣ الفلاتر المطبقة على البيانات

### على مستوى الواجهة الأمامية (Frontend)

#### يوزر COMMUNITY:
```typescript
// الشكاوى: يرى فقط شكاواه الشخصية
filteredComplaints = complaints.filter(c => c.userId === user.id);

// الطلبات: يرى فقط طلباته الشخصية
filteredRequests = requests.filter(r => r.userId === user.id);

// حسب التصنيف (classification):
if (maritime_agency) {
  // لا يرى طلبات المخازن
  filteredRequests = filteredRequests.filter(r => r.type !== 'warehouse');
}
if (shipping_company) {
  // لا يرى طلبات التراكي
  filteredRequests = filteredRequests.filter(r => r.type !== 'trolley');
}
```

#### Admin و DPA_STAFF:
```typescript
// يرى جميع الشكاوى والطلبات
// بدون فلاتر
```

#### Chairman:
```typescript
// يرى جميع الشكاوى والطلبات
// بدون تعديل (read-only)
```

---

## 6️⃣ المسارات (Routes) والصلاحيات

| المسار | ترجمة | Maritime Agency | Shipping | Admin | Staff | Chairman |
|--------|--------|:---------------:|:--------:|:-----:|:-----:|:--------:|
| `/dashboard` | لوحة التحكم | ✓ | ✓ | ✓ | ✓ | ✓ |
| `/map` | التجوال الافتراضي | ✓ | ✓ | ✓ | ✓ | ✓ |
| `/complaints` | الشكاوى والصيانة | ✓ | ✓ | ✓ | ✓ | ✓ |
| `/requests` | الطلبات | ✓ مع فلاتر | ✓ مع فلاتر | ✓ | ✓ | ✓ |
| `/requests/warehouse` | طلب مخزن | ✗ | ✓ | ✗ | ✗ | ✗ |
| `/requests/berthing` | طلب تراكي | ✓ | ✗ | ✗ | ✗ | ✗ |
| `/hydrography` | التحليلات | ✗ | ✗ | ✓ | ✓ | ✓ (read-only) |
| `/admin` | إدارة النظام | ✗ | ✗ | ✓ | ✗ | ✗ |
| `/profile` | الملف الشخصي | ✓ | ✓ | ✓ | ✓ | ✓ |

---

## 7️⃣ آليات الحماية المطبقة

### 🔐 على مستوى الواجهة (Frontend):

✅ **إخفاء العناصر:**
- الأزرار والقوائم الغير مسموحة تُخفى تماماً
- العناصر لا تظهر حتى لا تربك المستخدم

✅ **منع الوصول المباشر:**
- عند فتح رابط غير مسموح به → إعادة توجيه للداشبورد
- مثال: `/requests/warehouse` للوكيل الملاحي → 403 Forbidden

✅ **فلاتر حسب البيانات:**
- البيانات الحساسة فُلترت قبل العرض
- لا يمكن رؤية بيانات الآخرين

### 🔒 التوصيات على مستوى Backend (إن وُجد):

⚠️ **يجب تطبيق نفس الفلاتر على الخادم:**
```
GET /api/complaints
  → فقط شكاوى اليوزر الحالي
  
GET /api/requests
  → حسب التصنيف (classification)
  → فلاتر حسب الدور
```

---

## 8️⃣ خطوات الاختبار النهائي

### اختبار 1: وكيل ملاحي (Maritime Agency)
```
[ ] يرى الداشبورد
[ ] يقدر يضيف بلاغ صيانة
[ ] يرى بلاغاته فقط
[ ] يقدر يطلب تراكي (توكيل ملاحي)
[ ] لا يرى كلمة "تأجير مخزن"
[ ] لا يقدر يرتاح على /requests/warehouse
[ ] لا يرى التحليلات
[ ] تطلع feedback form بعد الإجراء
```

### اختبار 2: شركة شحن وتفريغ (Shipping Company)
```
[ ] يرى الداشبورد
[ ] يقدر يضيف بلاغ صيانة
[ ] يرى بلاغاته فقط
[ ] يقدر يطلب حجز مخزن
[ ] لا يرى كلمة "طلب تراكي"
[ ] لا يقدر يرتاح على /requests/berthing
[ ] لا يرى التحليلات
[ ] تطلع feedback form بعد الإجراء
```

### اختبار 3: مدير (Admin)
```
[ ] يرى كل الشكاوى وكل الطلبات
[ ] يقدر يغير حالة البلاغ (pending→in_progress→resolved)
[ ] يقدر يوافق/يرفض الطلبات
[ ] يرى لوحة الإدارة
[ ] يرى التحليلات كاملة
[ ] يقدر يديّر الحسابات
```

### اختبار 4: موظف (DPA Staff)
```
[ ] يرى كل الشكاوى وكل الطلبات
[ ] يقدر يغير حالة البلاغ
[ ] يقدر يوافق/يرفض الطلبات
[ ] لا يقدر يرتاح على /admin
[ ] يرى التحليلات كاملة
```

### اختبار 5: رئيس (Chairman)
```
[ ] يرى الداشبورد
[ ] يرى كل الشكاوى (read-only)
[ ] يرى كل الطلبات (read-only)
[ ] لا يقدر يعدّل أي حاجة
[ ] لا يقدر يضيف شكوى
[ ] لا يقدر يضيف طلب
[ ] يرى التحليلات (read-only)
```

---

## 9️⃣ الملفات المحدثة

### Frontend Files Modified:
- ✅ `src/contexts/DataContext.tsx` - تحديث نماذج البيانات
- ✅ `src/lib/rbac.ts` - إضافة دوال صلاحيات شاملة
- ✅ `src/pages/Dashboard.tsx` - تطبيق الفلاتر
- ✅ `src/pages/Complaints.tsx` - تطبيق الفلاتر
- ✅ `src/pages/Requests.tsx` - تطبيق الفلاتر
- ✅ `src/components/FeedbackModal.tsx` - نموذج التقييم

### Files Created:
- ✅ `AUDIT_REPORT_ROLES_PERMISSIONS.md` (هذا الملف)

---

## 🔟 الخلاصة

✅ **جميع المتطلبات مطبقة:**
- ✓ توزيع الصلاحيات حسب الدور
- ✓ فلاتر البيانات حسب المستخدم
- ✓ إخفاء العناصر غير المسموحة
- ✓ حالات البلاغات (pending/in_progress/resolved)
- ✓ حالات الطلبات محدثة
- ✓ نموذج التقييم والملاحظات
- ✓ توثيق شامل

**الحالة:** 🟢 **جاهز للإنتاج**

---

## 📞 الدعم والصيانة

لأي استفسارات أو تحديثات لاحقة:
- تراجع `src/lib/rbac.ts` للصلاحيات
- تراجع `src/contexts/DataContext.tsx` للبيانات
- راجع نماذج كل صفحة للفلاتر

**آخر تحديث:** 8 فبراير 2026
