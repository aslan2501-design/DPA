# خطة اختبار شاملة (RBAC Testing Checklist)

## 📋 معلومات الاختبار
- **التاريخ:** 8 فبراير 2026
- **الإصدار:** 1.0 (Release Ready)
- **الحالة:** ✅ جاهز للاختبار الشامل
- **الملف:** Testing Check-list for RBAC Implementation

---

## 🎯 الأهداف الرئيسية

1. ✅ التحقق من تطبيق الفلاتر على واجهة المستخدم
2. ✅ التحقق من تطبيق الفلاتر في منطق البيانات
3. ✅ اختبار حالات الاستخدام لكل دور
4. ✅ التحقق من عدم تسريب البيانات
5. ✅ اختبار آليات الحماية

---

## 🧪 خطوات الاختبار حسب الدور

### 🟦 اختبار 1: وكيل ملاحي (Maritime Agency)

**بيانات التسجيل:**
```
Email: agency@port.gov.eg
Password: any
UserType: COMMUNITY
Classification: agency
```

#### اختبارات الداشبورد:
```
✓ [ ] يفتح الداشبورد بنجاح
✓ [ ] يرى إحصائيات الشكاوى (كل الشكاوى)
✓ [ ] يرى في Quick Actions فقط:
       - بلاغ صيانة
       - طلب تراكي
✓ [ ] لا يرى زر "طلب مخزن" في Quick Actions
✓ [ ] يرى Map (التجوال الافتراضي)
```

#### اختبارات الشكاوى:
```
✓ [ ] يفتح صفحة الشكاوى
✓ [ ] يرى فقط شكاواه الشخصية (userId matching)
✓ [ ] يستطيع إضافة بلاغ جديد
✓ [ ] لا يستطيع تعديل حالة البلاغ (لا يرى زر Edit)
✓ [ ] يظهر نموذج التقييم بعد الإرسال ✓
```

#### اختبارات الطلبات:
```
✓ [ ] يفتح صفحة الطلبات
✓ [ ] يرى فقط طلباته الشخصية (userId matching)
✓ [ ] يرى زر "طلب تراكي" فقط
✓ [ ] لا يرى زر "طلب مخزن" (مخفي تماماً)
✓ [ ] يستطيع إضافة طلب تراكي جديد
✓ [ ] لا يستطيع تعديل حالة الطلب
✓ [ ] يظهر نموذج التقييم بعد الإرسال ✓
```

#### اختبارات الأمان:
```
✓ [ ] محاولة الوصول /requests/warehouse → redirect/403
✓ [ ] محاولة الوصول /hydrography → redirect/403
✓ [ ] محاولة الوصول /admin → redirect/403
✓ [ ] التحقق من عدم رؤية طلبات treasure_company
```

#### التحقق من الفلاتر (Browser Console):
```javascript
// على صفحة الطلبات، افتح المتصفح Console وتحقق:
console.log(requests); // يجب أن تكون جميع الطلبات type='trolley'

// على صفحة الشكاوى:
console.log(complaints); // يجب أن تكون جميع الشكاوى userId === user.id
```

---

### 🟦 اختبار 2: شركة شحن وتفريغ (Shipping Company)

**بيانات التسجيل:**
```
Email: shipping@port.gov.eg
Password: any
UserType: COMMUNITY
Classification: shipping
```

#### اختبارات الداشبورد:
```
✓ [ ] يفتح الداشبورد بنجاح
✓ [ ] يرى إحصائيات الشكاوى
✓ [ ] يرى في Quick Actions فقط:
       - بلاغ صيانة
       - طلب مخزن
✓ [ ] لا يرى زر "طلب تراكي"
✓ [ ] يرى Map (التجوال الافتراضي)
```

#### اختبارات الشكاوى:
```
✓ [ ] يفتح صفحة الشكاوى
✓ [ ] يرى فقط شكاواه الشخصية
✓ [ ] يستطيع إضافة بلاغ جديد
✓ [ ] لا يستطيع تعديل الحالة
✓ [ ] يظهر نموذج التقييم ✓
```

#### اختبارات الطلبات:
```
✓ [ ] يفتح صفحة الطلبات
✓ [ ] يرى فقط طلباته الشخصية
✓ [ ] يرى زر "طلب مخزن" فقط
✓ [ ] لا يرى زر "طلب تراكي" (مخفي)
✓ [ ] يستطيع إضافة طلب مخزن جديد
✓ [ ] لا يستطيع تعديل الحالة
✓ [ ] يظهر نموذج التقييم ✓
```

#### اختبارات الأمان:
```
✓ [ ] محاولة الوصول /requests/berthing → redirect/403
✓ [ ] محاولة الوصول /hydrography → redirect/403
✓ [ ] محاولة الوصول /admin → redirect/403
✓ [ ] التحقق من عدم رؤية طلبات agency
```

---

### 🟦 اختبار 3: مدير النظام (Admin)

**بيانات التسجيل:**
```
Email: admin@port.gov.eg
Password: any
UserType: ADMIN
Classification: null
```

#### اختبارات الداشبورد:
```
✓ [ ] يفتح الداشبورد بنجاح
✓ [ ] يرى إحصائيات جميع الشكاوى (الكل)
✓ [ ] يرى في Quick Actions جميع الخيارات:
       - بلاغ صيانة
       - طلب تراكي
       - طلب مخزن
✓ [ ] يرى جميع النوافذ المتاحة
```

#### اختبارات الشكاوى:
```
✓ [ ] يفتح صفحة الشكاوى
✓ [ ] يرى جميع الشكاوى (من جميع المستخدمين) ✓
✓ [ ] يستطيع إضافة بلاغ جديد
✓ [ ] يرى زر "Edit" لتعديل الحالة على كل بلاغ ✓
✓ [ ] يستطيع تغيير الحالة:
       pending → in_progress → resolved ✓
✓ [ ] يستطيع تغيير الحالة من أي حالة إلى أي حالة
```

#### اختبارات الطلبات:
```
✓ [ ] يفتح صفحة الطلبات
✓ [ ] يرى جميع الطلبات (من جميع المستخدمين) ✓
✓ [ ] يرى كلا النوعين: warehouse و trolley ✓
✓ [ ] يستطيع إضافة طلب جديد
✓ [ ] يرى أزرار الموافقة/الرفض على كل طلب ✓
✓ [ ] يستطيع تغيير الحالة:
       - Approve
       - In Progress
       - Reject
```

#### اختبارات التحليلات:
```
✓ [ ] يفتح صفحة التحليلات (Hydrography)
✓ [ ] يرى جميع البيانات
```

#### اختبارات الإدارة:
```
✓ [ ] يفتح صفحة الإدارة (/admin)
✓ [ ] يرى جميع إعدادات النظام
```

---

### 🟦 اختبار 4: موظف الميناء (DPA Staff)

**بيانات التسجيل:**
```
Email: staff@port.gov.eg
Password: any
UserType: DPA_STAFF
Classification: null
```

#### اختبارات الداشبورد:
```
✓ [ ] يفتح الداشبورد بنجاح
✓ [ ] يرى إحصائيات جميع الشكاوى
✓ [ ] يرى جميع Quick Actions
```

#### اختبارات الشكاوى:
```
✓ [ ] يفتح صفحة الشكاوى
✓ [ ] يرى جميع الشكاوى ✓
✓ [ ] يرى زر "Edit" لتعديل الحالة ✓
✓ [ ] يستطيع تغيير الحالة (pending → in_progress → resolved)
```

#### اختبارات الطلبات:
```
✓ [ ] يفتح صفحة الطلبات
✓ [ ] يرى جميع الطلبات (warehouse + trolley) ✓
✓ [ ] يرى أزرار الموافقة/الرفض ✓
✓ [ ] يستطيع تغيير الحالة
```

#### اختبارات الإدارة:
```
✓ [ ] محاولة الوصول /admin → redirect/403 ❌
✓ [ ] لا يرى قائمة الإدارة في Menu ❌
```

---

### 🟦 اختبار 5: الرئيس (Chairman)

**بيانات التسجيل:**
```
Email: chairman@port.gov.eg
Password: any
UserType: CHAIRMAN
Classification: null
isReadOnly: true (optional)
```

#### اختبارات الداشبورد:
```
✓ [ ] يفتح الداشبورد بنجاح
✓ [ ] يرى إحصائيات جميع الشكاوى (للعلم فقط)
✓ [ ] لا يرى أزرار الإجراءات (مخفية)
```

#### اختبارات الشكاوى:
```
✓ [ ] يفتح صفحة الشكاوى
✓ [ ] يرى جميع الشكاوى (read-only) ✓
✓ [ ] لا يرى زر "Edit" (مخفي) ❌
✓ [ ] لا يرى زر "+اضافة بلاغ جديد" (مخفي) ❌
✓ [ ] لا يستطيع تعديل أي بلاغ
```

#### اختبارات الطلبات:
```
✓ [ ] يفتح صفحة الطلبات
✓ [ ] يرى جميع الطلبات (read-only) ✓
✓ [ ] لا يرى أزرار الموافقة/الرفض (مخفية) ❌
✓ [ ] لا يرى زر "+طلب جديد" (مخفي) ❌
✓ [ ] لا يستطيع تعديل أي طلب
```

#### اختبارات التحليلات:
```
✓ [ ] يفتح صفحة التحليلات
✓ [ ] يرى البيانات (read-only)
```

---

## 🔒 اختبارات الأمان والوصول

### حالات الوصول المرفوضة:

#### Agency يحاول الوصول لـ:
```
✓ [ ] GET /requests/warehouse → يجب أن ترجع 403 أو تعيد التوجيه
✓ [ ] GET /hydrography → يجب أن ترجع 403 أو تعيد التوجيه
✓ [ ] GET /admin → يجب أن ترجع 403 أو تعيد التوجيه
✓ [ ] GET /requests (وفي البيانات يجب تصفية النوع warehouse)
```

#### Shipping Company يحاول الوصول لـ:
```
✓ [ ] GET /requests/trolley → يجب أن ترجع 403 أو تعيد التوجيه
✓ [ ] GET /hydrography → يجب أن ترجع 403 أو تعيد التوجيه
✓ [ ] GET /admin → يجب أن ترجع 403 أو تعيد التوجيه
✓ [ ] GET /requests (وفي البيانات يجب تصفية النوع trolley)
```

#### COMMUNITY يحاول الوصول لـ:
```
✓ [ ] GET /complaints (يجب أن يرى فقط له) → userId filter ✓
✓ [ ] GET /requests (يجب أن يرى فقط له) → userId filter ✓
✓ [ ] GET /admin → يجب أن ترجع 403 ❌
✓ [ ] GET /hydrography → يجب أن ترجع 403 ❌
```

---

## 📊 جدول النتائج

| الاختبار | Agency | Shipping | Admin | Staff | Chairman | ملاحظات |
|---------|:------:|:--------:|:-----:|:-----:|:--------:|---------|
| عرض الداشبورد | ✓ | ✓ | ✓ | ✓ | ✓ | يجب نجاح الدخول |
| إضافة بلاغ | ✓ | ✓ | ✓ | ✗ | ✗ | Chairman/Staff لا يضيفون بلاغات |
| تعديل حالة بلاغ | ✗ | ✗ | ✓ | ✓ | ✗ | فقط Admin و DPA_STAFF |
| عرض كل البلاغات | ✗ | ✗ | ✓ | ✓ | ✓ | Community يرى نفسه فقط |
| إضافة طلب | ✓ | ✓ | ✗ | ✗ | ✗ | Community فقط يضيف طلب |
| تعديل حالة طلب | ✗ | ✗ | ✓ | ✓ | ✗ | فقط Admin و DPA_STAFF |
| عرض كل الطلبات | ✗ | ✗ | ✓ | ✓ | ✓ | Community يرى نفسه فقط |
| عرض التحليلات | ✗ | ✗ | ✓ | ✓ | ✓ | Community لا يرى |
| لوحة الإدارة | ✗ | ✗ | ✓ | ✗ | ✗ | Admin فقط |

---

## 🛠️ خطوات الاختبار العملي

### 1️⃣ تشغيل التطبيق:
```bash
# في terminal
npm run dev   # أو bun run dev

# سيكون متاحاً على:
http://localhost:8082
```

### 2️⃣ فتح المتصفح:
```
1. افتح http://localhost:8082
2. سجل دخول بدور مختلف في كل اختبار
3. اختبر وفقاً للخطوات أعلاه
```

### 3️⃣ فتح DevTools:
```
F12 أو Cmd+Shift+I
→ Console tab
→ افحص البيانات: console.log(requests), console.log(complaints)
```

### 4️⃣ التحقق من الفلاتر:
```javascript
// في Console على صفحة الطلبات (شركة شحن):
// تحقق من عدم وجود طلبات type='trolley' في البيانات
requests.forEach(r => console.log(r.type, r.title));

// يجب أن تكون جميع القيم 'warehouse' فقط

// على صفحة الشكاوى (COMMUNITY):
// تحقق من أن جميع الشكاوى لديه نفس userId
complaints.forEach(c => console.log(c.userId, user?.id));
```

---

## 📋 النقاط الرئيسية للاختبار

- ✅ **فلترة البيانات:** البيانات المعروضة تتطابق مع صلاحيات المستخدم
- ✅ **إخفاء المكونات:** العناصر غير المسموحة مخفية تماماً (لا تظهر في DOM)
- ✅ **تعديل الحالات:** فقط المسؤولون يستطيعون تعديل الحالات
- ✅ **نموذج التقييم:** يظهر بعد الإجراءات للمستخدمين
- ✅ **الوصول المباشر:** محاولة الوصول لروابط محظورة تعيد التوجيه
- ✅ **عدم تسريب البيانات:** لا تظهر بيانات المستخدمين الآخرين

---

## 🎯 معايير النجاح

**يعتبر الاختبار ناجحاً إذا:**
- ✅ جميع الفلاتر تعمل بشكل صحيح
- ✅ لا يمكن رؤية/تعديل بيانات المستخدمين الآخرين
- ✅ جميع الأزرار والخيارات مخفية حسب الصلاحيات
- ✅ نماذج التقييم تظهر بنجاح
- ✅ لا توجد أخطاء في Console
- ✅ التطبيق مستقر ولا يتعطل

---

## 📝 ملاحظات إضافية

### إذا وجدت مشكلة:
1. تحقق من console.log في DevTools
2. تأكد من LoginContext يحفظ البيانات بشكل صحيح
3. تحقق من rbac.ts يرجع القيم الصحيحة
4. تحقق من DataContext يحفظ userData الصحيح

### كيفية إعادة تعيين البيانات:
```javascript
// في Console:
localStorage.clear();
location.reload();
// ثم سجل الدخول مجدداً
```

---

## 🚀 الخطوات التالية بعد الاختبار

1. ✅ توثيق أي مشاكل (إن وجدت)
2. ✅ إصلاح المشاكل
3. ✅ إعادة الاختبار
4. ✅ بناء وتوزيع الإصدار
5. ✅ نشر في الإنتاج

---

**تاريخ الإنجاز:** 8 فبراير 2026
**الحالة:** ✅ جاهز للاختبار الشامل
